# 개발 노트 (진행 상황 · 결정사항)

## ✅ Phase 1 — 계산 엔진 (완료)

TDD 원칙대로 화면 없이 계산 엔진만 먼저 구현하고 테스트로 검증했다.

### 만든 것
| 파일 | 역할 |
|------|------|
| `lib/scoring/types.ts` | 공통 타입 (재무제표, 비율코드, 반올림정책 등) |
| `lib/scoring/rounding.ts` | 절사/반올림/올림 + 안전한 나눗셈(0 방어). 반올림 **방식을 설정으로 분리** |
| `lib/scoring/ratios.ts` | 9개 재무비율 계산 + 산출비율 체이닝 |
| `lib/scoring/ratios.test.ts` | 스펙 5장 검증값 테스트 (**19개 전부 통과**) |

### 실행 방법
```bash
npm test        # 테스트 1회 실행
npm run test:watch   # 파일 저장 시 자동 재실행
```

### 핵심 발견 2가지 (스펙에 없던 사실)

**1. 산출비율은 "절사된 경영비율"을 업종평균으로 나눈다 (체이닝).**
스펙 4-2는 "자사 비율 ÷ 업종평균 × 100"만 적혀 있으나, 원본 프로그램을 역산하니
원본(무한소수) 비율이 아니라 **화면에 표시되는 절사된 비율**로 계산해야 값이 맞았다.
- 예) 차입금의존도: 원본 1.7738 → 절사 **1.77** → 1.77÷20.60×100 = **8.59** (✅ 원본 일치)
- 원본 1.7738로 계산하면 8.61 (❌ 불일치)

**2. ⚠️ 반올림 규칙이 단일하지 않다 — 예규 확인 필요.**
어떤 단일 규칙(순수 절사 / 순수 반올림)으로도 18개 값이 전부 일치하지 않는다.
현재 엔진은 스펙 기본값인 **절사(TRUNCATE, 소수 둘째 자리)** 로 동작하며 **14/18 정확 일치**.
아래 4개는 원본이 "올림"으로 표시되어 어긋난다(테스트에서 명시적으로 격리):

| 항목 | 실제 계산값 | 엔진(절사) | 원본 표시 |
|------|-----------|-----------|----------|
| 부채비율 (경영) | 47.1484 | 47.14 | **47.15** |
| 부채비율 (산출) | (경영에서 연쇄) | 44.92 | **44.93** |
| 매출순이익율 (산출) | 294.8775 | 294.87 | **294.88** |
| 자산회전율 (산출) | 134.3283 | 134.32 | **134.33** |

반대로 매출순이익율(경영) 13.2480→13.24, 현금흐름비율 23.0668→23.06 은 **절사**라야 맞다.
→ 원본이 항목별로 규칙이 다르거나, 부채비율만 원(₩) 단위 정밀도로 계산됐을 가능성.
→ **해결 방법**: 반올림 방식은 `RoundingPolicy`로 이미 분리해 두었으니, 정확한 규칙 확정 시
   정책값 변경(또는 항목별 오버라이드 추가)만으로 대응 가능.

### 확인이 필요한 질문 (사용자 → 예규/원본)
- [ ] 부채비율 등 4개 항목의 원본값(47.15 등)이 맞는지, 맞다면 반올림 규칙은?
- [ ] 조달청/행자부 예규상 절사 자리수·방식 규정 원문
- [ ] 산출비율 상한(cap) 존재 여부 (현재 904.31% 등 상한 없이 표시됨)

---

---

## ✅ Phase 2 — 데이터 입력 화면 (완료)

Supabase(클라우드) 대신 **회원가입 없이 쓰는 로컬 DB(SQLite)** 로 먼저 구현.
스키마는 Supabase(Postgres)로 이전 가능하도록 표/칸 이름을 스펙과 동일하게 유지.

### 만든 것
| 파일 | 역할 |
|------|------|
| `lib/db/schema.sql` | 스펙 3장 표 구조 (SQLite). 업체/재무제표/업종평균/등급구간/배점/신용/신인도/메모 |
| `lib/db/index.ts` | SQLite 연결(서버 전용) + 최초 실행 시 업종평균 시드 |
| `lib/db/repo.ts` | 데이터 접근 함수 (업체 CRUD, 재무제표 UPSERT, 업종평균 조회) |
| `app/api/companies/*` | REST API (목록/생성/조회/저장) |
| `components/Dashboard.tsx` | 메인 화면 [A]업체정보 + [D]재무제표 입력→실시간 9비율 계산 |
| `app/page.tsx`, `layout.tsx`, `globals.css` | Next.js 앱 셸 + 고밀도 스타일 |

### 실행 방법
```bash
npm run dev       # http://localhost:3000 (또는 지정 포트)
```
- DB 파일은 `data/app.db` 에 자동 생성 (git 미추적)

### 검증 완료
- 업체 생성(POST 201) → 재무제표 저장(PUT 200) → 재조회 시 값 보존 확인
- 화면에 [A][D] 패널 렌더, 재무수치 입력 시 브라우저에서 즉시 비율 계산

### 참고
- 계산은 **브라우저에서 lib/scoring 엔진으로 실시간** 수행, 저장 시에만 DB 기록
- 업종평균 시드값은 검증용(2026 예시) — 실제 공표 수치로 교체 필요

---

---

## ✅ Phase 3 + 원본화면 재현 + 파일기능 (완료)

### 원본 스크린샷과 동일한 고밀도 UI로 전면 재구성
`components/Dashboard.tsx` — 패널 [A]~[H]를 원본 프로그램 레이아웃대로 재현:
- [A] 업체 기본정보 (인라인 편집, 중소기업/여성대표 체크)
- [B] 보유면허 시공실적 + 기준월 탭 + **등급 1~5 드롭다운(회사별 직접 선택)**
- [C] 10년간 동일공종 실적합
- [D] 경영상태 평가자료 — 재무입력→9비율·**등급(A~E)** 실시간, 100억/10억↑/10억↓ 열
- [E] 신용등급 / [F] 기준별(조달청·행자부) 경영상태점수 / [G] 신인도 / [H] 메모·참고
- (참고: [B][C][E][G]는 현재 ㈜삼영기업 표시용 샘플 — DB 저장 연동은 다음 단계)

### 등급 엔진 (`lib/scoring/grades.ts`)
- `judgeGrade`: 산출비율→등급, **방향성(낮을수록/높을수록 우수) 반영**
- 예시 구간이 원본 화면 A등급 재현 (테스트 통과, 총 25개 green)
- ⚠️ 구간·배점은 예시값 — 실제 예규 확정 시 교체 (설정으로 분리됨)

### 파일 첨부 · 자동추출 · 미리보기 대조
- 상단 툴바 **📎 파일 첨부** (PDF/이미지)
- 업로드 시 **PDF 자동추출 시도 → 검증 팝업**에서 사람이 값 확인·수정 후 반영
  - 스캔 이미지본(예: 계림 확인서)은 텍스트가 없어 "직접 입력" 안내 (검증 완료)
  - 텍스트 있는 디지털 PDF는 라벨 매칭으로 재무필드 프리필
- **🔍 미리보기 모달**: 왼쪽 원본 문서(iframe/img) ↔ 오른쪽 입력된 재무값·비율 나란히 **대조**
- 파일 저장 `data/uploads/`, DB `company_files` 테이블

### 관련 파일
- `lib/extract/pdf.ts` (추출), `app/api/companies/[id]/files/*`, `app/api/files/[fileId]/*`
- `next.config.mjs`: better-sqlite3·pdf-parse를 서버 외부 패키지로 지정

---

## ⏭️ 다음 단계 (예정)
- [B][C][E][G] 패널 DB 저장/불러오기 연동 (현재 표시용 샘플)
- 기준월 스냅샷([B]), 공종 실적([C]) 실제 데이터 입력
- 등급 구간표·배점표 관리(편집) 화면 → 실제 예규 수치 반영
- 결과 PDF 요약 리포트 생성, 다중 업체 비교
- (선택) 스캔 PDF OCR 자동추출

> 미해결: 반올림 규칙 4개 항목(부채비율 등), 조달청/행자부 정확한 배점표·등급구간.

> 스펙 지시대로 각 Phase 종료 시 멈추고 보고한다.
