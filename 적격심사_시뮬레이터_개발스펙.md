# 건설업 입찰 적격심사 시뮬레이터 — 개발 스펙 (PRD)

> **이 문서의 용도**
> Claude Code / Cursor에 그대로 붙여넣어 개발을 진행하기 위한 상세 명세서.
> 작성자는 비개발자이므로, 구현 시 **용어와 판단 근거를 주석으로 남길 것**.

---

## 0. 한 줄 정의

건설회사가 공공 공사 입찰에 참여하기 전, **자사가 적격심사에서 몇 점을 받을지 미리 계산**해보는 데스크톱형 웹 애플리케이션.

---

## 1. 도메인 용어 사전 (개발자/AI가 반드시 먼저 읽을 것)

| 용어 | 의미 | 시스템에서의 역할 |
|---|---|---|
| **적격심사** | 최저가로 들어온 업체가 "이 공사를 감당할 능력이 있는가"를 점수로 검증하는 절차 | 이 프로그램의 최종 산출물 |
| **시공능력평가액 (시평액)** | 대한건설협회가 매년 공시하는 "이 회사가 1년에 소화 가능한 공사 규모" | 입찰 참가자격 1차 필터 |
| **업역 / 면허** | 토건, 토목, 건축, 산업환경, 전기, 정보통신 등 건설업 등록 종류 | 공사 종류별 참가 가능 여부 |
| **공종 (工種)** | 교통시설, 수자원시설, 기타토목, 주거시설, 비주거시설 등 공사 성격 분류 | 시공경험 평가 시 "동일공종 실적"만 인정 |
| **경영상태 평가** | 재무제표 기반 9개 재무비율로 재무건전성 채점 | 계산 엔진의 핵심 |
| **산출비율** | `자사 비율 ÷ 업종평균 비율 × 100` — 절대값이 아닌 **업계 평균 대비 상대 성적** | 등급 판정의 입력값 |
| **신인도** | 고용, 산업재해, 안전인증 등에 따른 **가점 / 감점** | 최종 점수에 가감 |
| **조달청 기준 / 행자부 기준** | 발주기관에 따라 배점표가 다름 (국가기관 vs 지방자치단체) | **같은 회사도 두 개의 점수가 나옴** |
| **추정가격 구간** | 100억↑ / 50억↑ / 30억↑ / 10억↑ / 10억↓ | 구간마다 배점표가 달라짐 |
| **기준월 (base_month)** | 시평액은 매년 7월 말 공시 → 8월 1일 갱신. 시점별 데이터가 다름 | **모든 실적 데이터는 기준월 스냅샷으로 보관** |

---

## 2. 기술 스택 (권장)

```
Frontend : Next.js 14 (App Router) + TypeScript + Tailwind CSS
그리드 UI : TanStack Table  또는  AG Grid Community
DB       : Supabase (PostgreSQL)
계산 엔진 : 순수 TypeScript 함수 (lib/scoring/ 하위, DB·UI 의존성 없음)
검증     : Zod (입력값 스키마 검증)
테스트   : Vitest
```

### 스택 선택 이유 (비개발자용 설명)

- **TanStack Table / AG Grid** — 원본 프로그램의 "엑셀 같은 격자 표" UI를 웹에서 재현하는 라이브러리. 직접 `<table>`로 만들면 셀 편집·키보드 이동을 전부 손으로 짜야 하는데, 이걸 대신 해줍니다.
- **계산 엔진을 순수 함수로 분리** — 화면이나 DB와 섞이지 않은 독립 함수로 만들라는 뜻. 그래야 "숫자만 넣으면 점수가 나오는지" 테스트로 검증할 수 있고, 나중에 화면을 갈아엎어도 계산 로직은 그대로 씁니다.
- **Zod** — 사용자가 숫자 칸에 글자를 넣는 등 잘못된 입력을 저장 전에 걸러주는 도구.

---

## 3. 데이터 구조 (DB 스키마)

> **설계 원칙 3가지 — 이걸 어기면 1년 뒤 프로그램을 다시 만들어야 합니다.**
> 1. **기준표(법령 수치)를 코드에 절대 하드코딩하지 말 것.** 반드시 별도 테이블. 매년 바뀝니다.
> 2. **모든 실적·재무 데이터는 "기준 시점"을 함께 저장.** 과거 시점 재계산이 가능해야 합니다.
> 3. **금액 단위는 전부 "천원" 정수**로 통일. 소수점 금액 금지 (부동소수점 오차 방지).

### 3-1. 업체 마스터

```sql
CREATE TABLE companies (
  id                BIGSERIAL PRIMARY KEY,
  name              TEXT NOT NULL,              -- 업체 상호
  biz_reg_no        TEXT UNIQUE NOT NULL,       -- 사업자 번호
  ceo_name          TEXT,                       -- 대표 이사
  address           TEXT,                       -- 사업자 주소
  phone             TEXT,
  fax               TEXT,
  is_sme            BOOLEAN DEFAULT FALSE,      -- 중소기업 여부 (가점 플래그)
  is_female_ceo     BOOLEAN DEFAULT FALSE,      -- 여성대표 여부 (가점 플래그)
  bid_manager       TEXT,                       -- 입찰 담당자
  biz_start_date    DATE,                       -- 영업일자 (건설업 등록일)
  region_date       DATE,                       -- 지역일자 (본점 소재지 이전일, 지역제한 입찰 판정용)
  audit_opinion     TEXT,                       -- 감사보고 의견 (적정/한정/부적정/의견거절)
  created_at        TIMESTAMPTZ DEFAULT now(),
  updated_at        TIMESTAMPTZ DEFAULT now()
);
```

### 3-2. 보유면허 및 시공실적 (기준월 스냅샷)

```sql
CREATE TABLE license_records (
  id                BIGSERIAL PRIMARY KEY,
  company_id        BIGINT REFERENCES companies(id) ON DELETE CASCADE,
  base_month        CHAR(7) NOT NULL,           -- '2026-07' 형식. 시평액 기준 시점
  license_type      TEXT NOT NULL,              -- 토건/토목/건축/산업환경/전기/정보통신
  grade             SMALLINT,                   -- 등급
  capacity_amount   BIGINT,                     -- 시공 능력액 (천원)
  perf_3y           BIGINT DEFAULT 0,           -- 3년간 실적 (천원)
  perf_5y           BIGINT DEFAULT 0,           -- 5년간 실적 (천원)
  perf_1y           BIGINT DEFAULT 0,           -- 최근 1년 실적 (천원)
  calc_date         DATE,                       -- 영업산정일
  license_reg_date  DATE,                       -- 면허등록일
  is_changed        BOOLEAN DEFAULT TRUE,       -- 직전 기준월 대비 변동 여부 (미변동 시 UI 연한색)
  UNIQUE (company_id, base_month, license_type)
);
```

### 3-3. 공종별 10년 실적

```sql
CREATE TABLE work_type_records (
  id                BIGSERIAL PRIMARY KEY,
  company_id        BIGINT REFERENCES companies(id) ON DELETE CASCADE,
  base_month        CHAR(7) NOT NULL,
  work_type         TEXT NOT NULL,              -- 교통시설/수자원시설/기타토목/주거시설/비주거시설
  amount_10y        BIGINT DEFAULT 0,           -- 10년 누계 실적 (천원)
  UNIQUE (company_id, base_month, work_type)
);
```

### 3-4. 재무제표 원천 데이터

```sql
CREATE TABLE financial_statements (
  id                    BIGSERIAL PRIMARY KEY,
  company_id            BIGINT REFERENCES companies(id) ON DELETE CASCADE,
  fiscal_year           SMALLINT NOT NULL,      -- 2026, 2025 ...
  -- 재무상태표
  total_liabilities     BIGINT,                 -- 부채 총계
  total_equity          BIGINT,                 -- 자기 자본
  current_assets        BIGINT,                 -- 유동 자산
  current_liabilities   BIGINT,                 -- 유동 부채
  borrowings            BIGINT,                 -- 차입금
  total_assets          BIGINT,                 -- 총 자산
  avg_total_assets      BIGINT,                 -- 기초기말평균자산
  -- 손익계산서
  operating_profit      BIGINT,                 -- 영업 이익
  interest_expense      BIGINT,                 -- 이자 비용
  net_profit            BIGINT,                 -- 순 이익
  total_revenue         BIGINT,                 -- 총 매출액
  -- 현금흐름표
  operating_cashflow    BIGINT,                 -- 영업현금흐름
  -- 건설업 특수 항목
  construction_invest   BIGINT DEFAULT 0,       -- 건설개발투자
  construction_revenue  BIGINT DEFAULT 0,       -- 건설 매출액
  UNIQUE (company_id, fiscal_year)
);
```

### 3-5. 업종평균 비율 (기준표 ①)

```sql
CREATE TABLE industry_averages (
  id                BIGSERIAL PRIMARY KEY,
  fiscal_year       SMALLINT NOT NULL,
  industry_code     TEXT NOT NULL,              -- 'CONSTRUCTION_GENERAL' 등
  ratio_code        TEXT NOT NULL,              -- 아래 RATIO_CODES 참조
  avg_value         NUMERIC(12,4) NOT NULL,     -- 업종 평균값 (% 또는 배/회)
  source            TEXT,                       -- 출처 (예: '2026 조달청 공표')
  UNIQUE (fiscal_year, industry_code, ratio_code)
);
```

### 3-6. 등급 구간표 (기준표 ②)

```sql
CREATE TABLE ratio_grade_bands (
  id                BIGSERIAL PRIMARY KEY,
  effective_year    SMALLINT NOT NULL,          -- 적용 연도
  agency            TEXT NOT NULL,              -- 'PPS'(조달청) | 'MOIS'(행자부)
  scale_band        TEXT NOT NULL,              -- 'OVER_100E' | 'OVER_10E' | 'UNDER_10E'
  ratio_code        TEXT NOT NULL,
  min_calc_ratio    NUMERIC(12,4),              -- 산출비율 하한 (NULL = 무제한)
  max_calc_ratio    NUMERIC(12,4),              -- 산출비율 상한 (NULL = 무제한)
  grade             CHAR(1) NOT NULL,           -- A~E
  points            NUMERIC(6,3)                -- 해당 등급 배점
);
```

### 3-7. 기준별 배점표 (기준표 ③)

```sql
CREATE TABLE scoring_rules (
  id                BIGSERIAL PRIMARY KEY,
  effective_year    SMALLINT NOT NULL,
  agency            TEXT NOT NULL,              -- 'PPS' | 'MOIS'
  scale_band        TEXT NOT NULL,              -- 'PRE_SCREEN'(사전심사) | 'SIMPLE'(간이종심) | 'OVER_50E' | 'OVER_30E' | 'OVER_10E' | 'UNDER_10E'
  total_points      NUMERIC(6,3),               -- 배점 (예: 35.0)
  finance_points    NUMERIC(6,3),               -- 재무 만점
  credit_points     NUMERIC(6,3)                -- 신용 만점
);
```

### 3-8. 신용등급 이력

```sql
CREATE TABLE credit_ratings (
  id                BIGSERIAL PRIMARY KEY,
  company_id        BIGINT REFERENCES companies(id) ON DELETE CASCADE,
  credit_type       TEXT NOT NULL,              -- '기업신용' | '회사채' | '기업어음'
  grade             TEXT NOT NULL,              -- 'A+' 등
  score             NUMERIC(6,3),               -- 환산 점수 (예: 35.0)
  eval_date         DATE NOT NULL,              -- 평가일
  expiry_date       DATE NOT NULL,              -- 유효일
  note              TEXT
);
```

### 3-9. 신인도 항목

```sql
CREATE TABLE reliability_items (
  id                BIGSERIAL PRIMARY KEY,
  company_id        BIGINT REFERENCES companies(id) ON DELETE CASCADE,
  item_code         TEXT NOT NULL,              -- 'EMPLOYMENT_FLEX' | 'FATALITY_RATE' | 'DISASTER_PREV' | 'SAFETY_CERT'
  item_name         TEXT NOT NULL,              -- 고용탄력성 / 사망만인율 / 재해예방 / 안전인증
  pps_score         NUMERIC(6,3) DEFAULT 0,     -- 조달청 기준 가감점
  mois_score        NUMERIC(6,3) DEFAULT 0,     -- 행자부 기준 가감점
  apply_from        DATE NOT NULL,              -- 적용일
  apply_to          DATE NOT NULL,              -- 만료일
  note              TEXT                        -- '4등급', '95점', 'A' 등
);
```

### 3-10. 메모

```sql
CREATE TABLE company_memos (
  id                BIGSERIAL PRIMARY KEY,
  company_id        BIGINT REFERENCES companies(id) ON DELETE CASCADE,
  memo              TEXT,
  reference         TEXT,                       -- 참고
  updated_at        TIMESTAMPTZ DEFAULT now()
);
```

---

## 4. 계산 엔진 명세 ★ 가장 중요

### 4-1. 9개 재무비율 정의

`lib/scoring/ratios.ts`

| # | ratio_code | 표시명 | 계산식 | 단위 | 방향성 |
|---|---|---|---|---|---|
| 1 | `DEBT_RATIO` | 부채 비율 | 부채총계 ÷ 자기자본 × 100 | % | **낮을수록 우수** |
| 2 | `CURRENT_RATIO` | 유동 비율 | 유동자산 ÷ 유동부채 × 100 | % | 높을수록 우수 |
| 3 | `BORROWING_DEP` | 차입금의존도 | 차입금 ÷ 총자산 × 100 | % | **낮을수록 우수** |
| 4 | `INTEREST_COV` | 이자보상배율 | 영업이익 ÷ 이자비용 | 배 | 높을수록 우수 |
| 5 | `NET_MARGIN` | 매출순이익율 | 순이익 ÷ 총매출액 × 100 | % | 높을수록 우수 |
| 6 | `ROA` | 자산순이익율 | 순이익 ÷ 총자산 × 100 | % | 높을수록 우수 |
| 7 | `CASHFLOW_RATIO` | 현금흐름비율 | 영업현금흐름 ÷ 총자산 × 100 | % | 높을수록 우수 |
| 8 | `ASSET_TURNOVER` | 자산회전율 | 매출액 ÷ 기초기말평균자산 | 회 | 높을수록 우수 |
| 9 | `CONST_INVEST` | 건설투자비율 | 건설개발투자 ÷ 건설매출액 × 100 | % | 높을수록 우수 |

### 4-2. 산출비율

```
산출비율(%) = (자사 비율 ÷ 업종평균 비율) × 100
```

### 4-3. 반올림 규칙 ★ 실제 화면에서 역산 확인 완료

원본 프로그램의 숫자를 역산한 결과, **소수점 셋째 자리에서 버림(절사)** 규칙을 사용합니다. 반올림이 아닙니다.

검증 근거:
- 매출순이익율: `32,540,776 ÷ 245,628,105 = 13.2480…%` → 화면 **13.24%** (반올림이면 13.25가 되어야 함)
- 현금흐름비율: `65,019,595 ÷ 281,875,837 = 23.0668…%` → 화면 **23.06%** (반올림이면 23.07)
- 자산회전율: `245,628,105 ÷ 270,043,947 = 0.9096…회` → 화면 **0.90회**

```ts
/**
 * 소수점 n자리에서 버림(절사).
 * 적격심사는 반올림이 아닌 절사가 표준이므로 Math.round를 쓰면 안 됩니다.
 * 부동소수점 오차를 피하기 위해 곱하기 → floor → 나누기 순서로 처리합니다.
 */
export function truncate(value: number, digits = 2): number {
  const factor = 10 ** digits;
  return Math.floor(value * factor + 1e-9) / factor;
}
```

> ⚠️ **주의**: 절사/반올림 자리수는 발주처 예규에 따라 다를 수 있습니다.
> `system_settings` 테이블에 `rounding_mode`(TRUNCATE | ROUND | CEIL)와 `rounding_digits`를 두어 **설정으로 변경 가능하게** 만드세요.

### 4-4. 0 나누기 방어

분모가 0 또는 NULL인 경우가 실제로 존재합니다 (이자비용 0, 건설매출액 0 등).

```ts
export function safeDivide(numerator: number, denominator: number): number | null {
  if (!denominator || denominator === 0) return null;  // null = '해당없음'
  return numerator / denominator;
}
```

- 결과가 `null`이면 UI에 `0.00%` 또는 `-` 로 표시하고, 등급은 최하위(E) 또는 "평가제외"로 처리
- 화면 예시의 건설투자비율이 `0.00% / E등급`인 것이 이 케이스

### 4-5. 등급 판정 로직

```ts
type Direction = 'HIGHER_BETTER' | 'LOWER_BETTER';

/**
 * 산출비율을 등급 구간표(ratio_grade_bands)에 대입해 등급과 배점을 반환.
 * 구간표는 반드시 DB에서 조회할 것. 코드에 숫자를 박지 마세요.
 */
function judgeGrade(
  calcRatio: number,
  bands: RatioGradeBand[]   // agency + scale_band + ratio_code 로 필터링된 구간표
): { grade: string; points: number } { ... }
```

### 4-6. 최종 경영상태 점수

```
경영상태점수 = MIN(재무점수, 재무만점) + MIN(신용점수, 신용만점)
최종점수     = 경영상태점수 + 시공경험점수 + 신인도가감점
```

- **조달청 / 행자부 각각 별도로 계산**하여 두 결과를 동시에 표시
- 각 기관 × 각 추정가격 구간마다 결과가 다르므로, **결과는 매트릭스(표) 형태**로 출력

---

## 5. 계산 엔진 테스트 케이스 (필수)

> Claude Code에게: **아래 테스트를 먼저 작성하고, 통과하는 코드를 만드세요 (TDD).**
> 이 숫자들은 실제 프로그램 화면에서 검증된 값입니다.

### 입력 데이터 (단위: 천원)

```ts
const testFinancials = {
  total_liabilities:    90_316_995,
  total_equity:        191_558_842,
  current_assets:      224_156_743,
  current_liabilities:  88_094_288,
  borrowings:            5_000_000,
  total_assets:        281_875_837,
  avg_total_assets:    270_043_947,
  operating_profit:     20_823_688,
  interest_expense:      1_111_633,
  net_profit:           32_540_776,
  total_revenue:       245_628_105,
  operating_cashflow:   65_019_595,
  construction_invest:           0,
  construction_revenue:236_641_846,
};

const testIndustryAverages = {
  DEBT_RATIO:     104.94,
  CURRENT_RATIO:  139.21,
  BORROWING_DEP:   20.60,
  INTEREST_COV:     3.35,
  NET_MARGIN:       4.49,
  ROA:              2.88,
  CASHFLOW_RATIO:   2.55,
  ASSET_TURNOVER:   0.67,
  CONST_INVEST:     0.08,
};
```

### 기대 출력

| ratio_code | 경영비율 (기대값) | 산출비율 (기대값) |
|---|---|---|
| DEBT_RATIO | 47.15 % | 44.93 % |
| CURRENT_RATIO | 254.45 % | 182.78 % |
| BORROWING_DEP | 1.77 % | 8.59 % |
| INTEREST_COV | 18.73 배 | 559.10 % |
| NET_MARGIN | 13.24 % | 294.88 % |
| ROA | 11.54 % | 400.69 % |
| CASHFLOW_RATIO | 23.06 % | 904.31 % |
| ASSET_TURNOVER | 0.90 회 | 134.33 % |
| CONST_INVEST | 0.00 % | 0.00 % |

**이 표의 18개 숫자가 전부 정확히 일치해야 계산 엔진이 완성된 것입니다.**

---

## 6. 화면 명세

### 6-1. 전체 레이아웃

원본과 동일한 **고밀도 정보 표시형(dense) 단일 화면**. 스크롤 없이 한눈에 전체가 보이는 것이 이 프로그램의 핵심 UX입니다.

```
┌──────────────────────────────────────────────────────────────┐
│ [A] 업체 기본정보                                              │
├────────────────────────────────────┬─────────────────────────┤
│ [B] 보유면허 시공실적 (기준월 탭)      │ [C] 10년간 동일공종 실적합 │
├────────────────────────────────────┴─────────────────────────┤
│ [D] 경영상태 평가자료 (9행 × 재무원천 | 비율 | 산출 | 등급)      │
├────────────────────────────────────┬─────────────────────────┤
│ [E] 신용등급 평가자료                │ [F] 기준별 경영상태점수    │
├────────────────────────────────────┼─────────────────────────┤
│ [G] 신인도 평가자료                  │ [H] 메모 / 참고           │
└────────────────────────────────────┴─────────────────────────┘
```

### 6-2. 패널별 요구사항

**[A] 업체 기본정보**
- 인라인 편집 가능한 입력 필드
- 중소기업 / 여성대표는 체크박스
- 사업자번호 형식 검증 (`###-##-#####`)

**[B] 보유면허 시공실적**
- 상단에 **기준월 탭** (최근 4개월). 클릭 시 해당 스냅샷 로드
- 선택된 탭은 노란색 하이라이트
- 행 클릭 시 파란색 선택 표시
- 금액 3자리 콤마, 우측 정렬
- `is_changed = false`인 행은 연한 회색 (원본의 "실적 변경안된 경우 연한색" 규칙)
- `[업역면허조회]` 버튼 → Phase 4에서 외부 연동

**[D] 경영상태 평가자료** ← 가장 복잡
- 왼쪽: 재무제표 원천 입력 (숫자 입력, 천원 단위)
- 오른쪽: 계산 결과 (읽기 전용, 자동 갱신)
- 연도 탭 (2026 / 2025) — 전년 대비 비교
- 등급 색상: A=파랑, B=초록, C=검정, D=주황, E=빨강
- **입력값이 바뀌면 즉시 재계산** (debounce 300ms)

**[F] 기준별 경영상태점수**
- 조달청 / 행자부 2개 블록
- 각 블록에 추정가격 구간별 행 × (배점 / 재무 / 신용) 열
- 만점 대비 부족분은 빨간색 강조

**[G] 신인도 평가자료**
- 조달 / 행자 점수를 별도 열로 표시
- 만료일이 지난 항목은 회색 처리 + 자동 제외
- `[추가 / 삭제]` 버튼

### 6-3. 공통 UI 규칙

| 항목 | 규칙 |
|---|---|
| 폰트 | 시스템 고정폭 계열, 12px, 숫자는 tabular-nums |
| 숫자 정렬 | 우측 정렬, 3자리 콤마 |
| 음수 | 빨간색 (예: 유효기간 `-43`) |
| 키보드 | Tab = 다음 셀, Enter = 아래 행, Esc = 편집 취소 |
| 자동저장 | 셀 포커스 아웃 시 저장 |
| 단축키 | Ctrl+S 저장, Ctrl+P 인쇄 |

---

## 7. 개발 단계 (Phase)

### Phase 1 — 계산 엔진 (1주)
- [ ] 프로젝트 셋업 (Next.js + TS + Tailwind)
- [ ] `lib/scoring/ratios.ts` — 9개 비율 계산 함수
- [ ] `lib/scoring/truncate.ts` — 절사 유틸
- [ ] **5장 테스트 케이스 18개 전부 통과**
- [ ] 이 단계에서 화면은 만들지 않음. 콘솔/테스트로만 검증

### Phase 2 — 데이터 입력 화면 (2주)
- [ ] Supabase 스키마 생성 + 시드 데이터
- [ ] 패널 [A] 업체정보 CRUD
- [ ] 패널 [D] 재무제표 입력 → 실시간 비율 계산 표시
- [ ] 기준표 관리 화면 (업종평균 비율 입력)

### Phase 3 — 등급 · 점수 (2주)
- [ ] 등급 구간표 입력 화면
- [ ] `judgeGrade` 구현 + 패널 [D] 등급 열 표시
- [ ] 패널 [E] 신용등급, [G] 신인도
- [ ] 패널 [F] 기준별 점수 매트릭스

### Phase 4 — 실적 · 이력 (1주)
- [ ] 패널 [B] 기준월 스냅샷 + 탭 전환
- [ ] 패널 [C] 공종별 10년 실적
- [ ] 패널 [H] 메모

### Phase 5 — 확장 (선택)
- [ ] KISCON / 나라장터 데이터 조회 연동
- [ ] 결과 PDF · Excel 내보내기
- [ ] 다중 업체 비교 (경쟁사 시뮬레이션)
- [ ] "부채비율을 몇 %까지 낮추면 A등급" 역산 시뮬레이터

---

## 8. Claude Code 작업 지시사항

```
이 문서를 읽고 Phase 1부터 순서대로 진행해줘.

지켜야 할 것:
1. Phase 1이 끝나기 전에 UI 코드를 작성하지 마. 계산 엔진 테스트가 전부 통과한 뒤에 화면으로 넘어갈 것.
2. 5장의 테스트 케이스 18개를 Vitest 테스트 파일로 먼저 작성하고, 그 다음에 구현해.
3. 법령 기준 수치(업종평균, 등급구간, 배점)는 절대 코드에 하드코딩하지 말고 DB 테이블 또는 seed 파일로 분리해.
4. 금액은 전부 정수(천원 단위)로 다뤄. float 금액 금지.
5. 나눗셈은 반드시 safeDivide를 거쳐서 0 나누기를 방어해.
6. 반올림이 아니라 절사(truncate)를 쓰되, 자리수와 방식은 설정값으로 뺄 것.
7. 각 함수 위에 "이 함수가 도메인에서 무슨 의미인지" 한글 주석을 달아줘.
8. 각 Phase가 끝날 때마다 멈추고, 무엇을 만들었고 다음에 뭘 할지 보고해.
```

---

## 9. 미확정 / 확인 필요 사항

이 항목들은 **실제 법령·예규 원문을 확인해야** 정확한 값을 넣을 수 있습니다. 개발은 구조만 잡고 진행하되, 실제 사용 전 반드시 검증하세요.

- [ ] 조달청 「시설공사 적격심사 세부기준」 최신 개정본의 경영상태 배점표
- [ ] 행정안전부 「지방자치단체 입찰시 낙찰자 결정기준」 최신 개정본
- [ ] 업종평균 재무비율 공표 수치 (연도별, 업종별)
- [ ] 신용평가등급 → 점수 환산표
- [ ] 신인도 가감점 항목별 정확한 배점
- [ ] 절사 자리수 규정 (본 문서는 화면 역산으로 "셋째 자리 절사" 추정)
- [ ] 산출비율 상한(cap) 존재 여부 — 화면상 904.31%가 그대로 표시되므로 상한 없음으로 보이나, 등급 판정 시 상한이 있을 수 있음

> 현행 기준은 조달청 누리집 「법령·훈령·예규」와 국가법령정보센터에서 확인 가능합니다.
